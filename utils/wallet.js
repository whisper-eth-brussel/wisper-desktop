// sign tx
// generate key pair if not exists
// hash

function signTx() {
  // hash falan da yap

  return signedTransaction; // string
}

function getPubkey() {
  return "asd";
}

module.exports = {
  signTx,
};
